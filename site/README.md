# Digital Genealogist — Netlify site

Drag this folder onto https://app.netlify.com/drop to deploy.

Or with the Netlify CLI:

```
npm install -g netlify-cli
cd site
netlify deploy --prod
```

Pure static site — no build step required.
